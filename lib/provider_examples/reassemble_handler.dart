import 'package:flutter/material.dart';

class ReassembleHandler extends StatelessWidget {
  final Locale locale;

  const ReassembleHandler({super.key, required this.locale});

  @override
  Widget build(BuildContext context) {
    return Text(runtimeType.toString());
  }
}
