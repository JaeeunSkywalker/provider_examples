import 'package:flutter/material.dart';

class Selector0 extends StatelessWidget {
  final Locale locale;

  const Selector0({super.key, required this.locale});

  @override
  Widget build(BuildContext context) {
    return Text(runtimeType.toString());
  }
}
