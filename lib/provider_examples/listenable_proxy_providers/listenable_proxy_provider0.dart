import 'package:flutter/material.dart';

class ListenableProxyProvider0 extends StatelessWidget {
  final Locale locale;

  const ListenableProxyProvider0({super.key, required this.locale});

  @override
  Widget build(BuildContext context) {
    return Text(runtimeType.toString());
  }
}
