import 'package:flutter/material.dart';

class ListenableProxyProvider6 extends StatelessWidget {
  final Locale locale;

  const ListenableProxyProvider6({super.key, required this.locale});

  @override
  Widget build(BuildContext context) {
    return Text(runtimeType.toString());
  }
}
