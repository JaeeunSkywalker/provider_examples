import 'package:flutter/material.dart';

class ProxyProvider0 extends StatelessWidget {
  final Locale locale;

  const ProxyProvider0({super.key, required this.locale});

  @override
  Widget build(BuildContext context) {
    return Text(runtimeType.toString());
  }
}
