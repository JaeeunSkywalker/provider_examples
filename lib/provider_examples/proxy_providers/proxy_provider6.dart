import 'package:flutter/material.dart';

class ProxyProvider6 extends StatelessWidget {
  final Locale locale;

  const ProxyProvider6({super.key, required this.locale});

  @override
  Widget build(BuildContext context) {
    return Text(runtimeType.toString());
  }
}
