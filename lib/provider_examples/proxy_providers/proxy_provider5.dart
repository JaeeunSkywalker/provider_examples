import 'package:flutter/material.dart';

class ProxyProvider5 extends StatelessWidget {
  final Locale locale;

  const ProxyProvider5({super.key, required this.locale});

  @override
  Widget build(BuildContext context) {
    return Text(runtimeType.toString());
  }
}
