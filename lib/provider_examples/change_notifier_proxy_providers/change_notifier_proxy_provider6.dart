import 'package:flutter/material.dart';

class ChangeNotifierProxyProvider6 extends StatelessWidget {
  final Locale locale;

  const ChangeNotifierProxyProvider6({super.key, required this.locale});

  @override
  Widget build(BuildContext context) {
    return Text(runtimeType.toString());
  }
}
