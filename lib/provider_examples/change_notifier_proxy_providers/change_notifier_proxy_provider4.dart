import 'package:flutter/material.dart';

class ChangeNotifierProxyProvider4 extends StatelessWidget {
  final Locale locale;

  const ChangeNotifierProxyProvider4({super.key, required this.locale});

  @override
  Widget build(BuildContext context) {
    return Text(runtimeType.toString());
  }
}
